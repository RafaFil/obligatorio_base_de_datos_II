const findByCredentials = async (username, hashpwd) => {
    return true;
}

const findByUsername = async (username) => {
    return true;
}

module.exports = {
    findByCredentials,
    findByUsername
}